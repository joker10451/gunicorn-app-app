services:
  - type: web
    name: leadgid-landing
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: LEADGID_TOKEN
        sync: false
      - key: LEADGID_OFFER_ID
        value: ""
